// main.js
import { createFooter } from './footer.js';

document.addEventListener('DOMContentLoaded', function() {
  createFooter(); // Call the createFooter function to create the footer dynamically
});